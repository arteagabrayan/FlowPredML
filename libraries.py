import numpy as np
import pandas as pd
import sklearn
import streamlit as st
import seaborn as sns
import matplotlib
from PIL import Image, __version__ as pillow_version
#import pickle

print(f'Versión de Numpy: {np.__version__}')
print(f'Versión de Pandas: {pd.__version__}')
print(f'Versión de scikit-learn: {sklearn.__version__}')
print(f'Versión de Streamlit: {st.__version__}')
print(f'Versión de Seaborn: {sns.__version__}')
print(f'Versión de Matplotlib: {matplotlib.__version__}')
print(f'Versión de PIL (Pillow): {pillow_version}')
#print(f'Versión de Pickle (parte de la biblioteca estándar de Python): {pickle.format_version}')
